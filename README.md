My implementations of projects from The Rust Programming Language book as I learn Rust.
Each project is in its own directory. Navigate to any project and run with cargo run.
Requires Rust 1.70+ (install at https://rustup.rs/)